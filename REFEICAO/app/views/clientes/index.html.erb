<% content_for :titlePage do %>
  Lista de Clientes
<% end %>

<%=render '/layouts/panel' %>

<div class="containermininmo">
  <table class="table table-hover">
  <thead>
    <tr>
      <th>Nome</th>
      <th>CPF</th>
      <th>E-mail</th>
      <th>Sessão</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @clientes.each do |cliente| %>
      <tr>
        <td><%= cliente.DESC_Nome %></td>
        <td><%= cliente.NUMR_CPF %></td>
        <td><%= cliente.DESC_Email %></td>
        <td><%= cliente.DESC_Secao %></td>
        <td><%= link_to image_tag('show.png', :title => 'Exibir', class: 'exibirico') , cliente %></td>
        <td><%= link_to image_tag('edit.png', :title => 'Editar',  class: 'exibirico') , edit_cliente_path(cliente) %></td>
        <td><%= link_to image_tag('trash.png', :title => 'Excluir',  class: 'exibirico'), cliente , :method => :delete,  :confirm => 'Deseja excluir o registro?'%></td>         
      </tr>
    <% end %>
  </tbody>
</table>

</div>
</div></div>
<table style="margin-top:10px;">
  <tr>
    <td align="center" style="width:50px;">
      <%= link_to image_tag('new.png', :title => 'Novo', class: 'exibirico') , new_cliente_path %>
    </td>
  </tr>
  <tr>
    <td align="center" style="width:20px;">
      <label class="altBotoes">Novo</label>
    </td>
  </tr> 
</table>
